#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(){
    char nome[30];
    printf("\nInforme o nome: ");
    gets(nome);
    for (int i = 0; i < strlen(nome); i++)
    {
        nome[i] = tolower(nome[i]);
    }
    printf("\nO nome em minúsculo é: %s",nome);
    return 0;
}
